		.data #定义数据段
Array:  .word 10, 13, 35, 48 #数组赋值
		.text #定义代码段
		la $11, Array #伪指令 将数组基址存入$11
			
		addi $10, $0, 10     #N=10           
		add $8, $0, $0      #i=0          
		add $14, $0, $0     #与“数组基地址”的字节数偏移量初值=0          
Loop:	add $15, $11, $14   # $15=&A[i]
		lw    $12, 0($15)   # $12=A[i]
		add $9, $9, $12     # g = g + A[i]
		addi $8, $8, 1	      # i = i + 1
		addi $14, $14, 4    #下一个数组元素与与数组基地址的字节数偏移量
		bne  $8, $10, Loop
Else:   # 。。。其它指令，省略